services:
  - type: web
    name: m268-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard_server.py 10000
    envVars:
      - key: DASHBOARD_PASSWORD
        sync: false
